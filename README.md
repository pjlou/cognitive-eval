# cognitive-eval
an open-source evaluation suite using Python and spaCy to diagnose structural semantic failure modes (quantifier scope ambiguity, complex negation, compositional logic) in large language models
